package com.example.contract.generated

import com.example.contract.NoOptionalProperty
import kotlin.String
import kotlin.Suppress
import net.ntworld.foundation.Faker

class NoOptionalPropertyFactory(
  private val faker: Faker
) {
  fun make(
    id: String,
    email: String,
    firstName: String,
    lastName: String
  ): NoOptionalProperty = NoOptionalPropertyImpl(
    id = id,
    email = email,
    firstName = firstName,
    lastName = lastName
  )

  @Suppress("UNCHECKED_CAST")
  private fun <T> createFakedData(type: String): T = faker.makeFakeData(type) as T
}
